<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".HostApp"
        android:allowBackup="true"
        android:configChanges="screenSize|keyboardHidden|orientation|keyboard"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:windowSoftInputMode="adjustPan|stateHidden"
        android:preserveLegacyExternalStorage="true"
        tools:replace="android:allowBackup"
        android:theme="@style/Theme.Ferrostar">
        <provider
            android:name=".HostProvider"
            android:authorities="com.combo.plugin.sample.provider"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedContentProvider" />

        <receiver
            android:name=".HostReceiver"
            android:enabled="true"
            android:exported="true"
            tools:ignore="ExportedReceiver">
            <intent-filter>
                <action android:name="com.combo.plugin.example.action.STATIC_BROADCAST" />
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.USER_PRESENT" />
                <action android:name="android.bluetooth.adapter.action.STATE_CHANGED" />
                <action android:name="android.intent.action.AIRPLANE_MODE" />
                <action android:name="android.intent.action.HEADSET_PLUG" />
                <action android:name="android.intent.action.LOCALE_CHANGED" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.DATE_CHANGED" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <action android:name="android.intent.action.PACKAGE_REPLACED" />
                <action android:name="android.intent.action.PACKAGE_REMOVED" />

                <data android:scheme="package" />
            </intent-filter>
        </receiver>

        <service
            android:name=".HostService1"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />
        <service
            android:name=".HostService2"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />
        <service
            android:name=".HostService3"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />
        <service
            android:name=".HostService4"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />
        <service
            android:name=".HostService5"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />
        <service
            android:name=".HostService6"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />
        <service
            android:name=".HostService7"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />
        <service
            android:name=".HostService8"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />
        <service
            android:name=".HostService9"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />
        <service
            android:name=".HostService10"
            android:foregroundServiceType="dataSync|mediaPlayback|shortService" />

        <activity
            android:name=".HostActivity"
            android:exported="true"
            android:networkSecurityConfig="@xml/network_security_config"
            android:theme="@style/Theme.Ferrostar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>